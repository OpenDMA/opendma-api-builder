/**
 * OdmaQuerySyntaxError
 * 
 * Thrown when the backend service failed.
 */
class OdmaServiceError extends OdmaError {

    /**
     * Create a new OdmaServiceError.
     * 
     * @param {string} message - The error message.
     */
    constructor(message = "Backend service failed") {
        super(message);
    }

}
