[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "opendma-api"
version = "{{version}}"
dependencies = []
python_requires = ">=3.7"
