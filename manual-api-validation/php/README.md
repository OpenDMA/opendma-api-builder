# Manual validation of generated PHP API

Validation steps:
1. Follow the steps in https://getcomposer.org/download/ to install composer locally into this folder
2. Run `php composer.phar install`. This will create a folder named `vendor` and copy the opendma-api into it
3. Run `php sample.php` to run this sample